import { formatIngredient } from '../../src/data/recipes-product.js';

const escapeHtml = (value) => String(value ?? '').replace(/[&<>"']/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[character]);
const safeJson = (value) => JSON.stringify(value).replace(/</g, '\\u003c').replace(/>/g, '\\u003e').replace(/&/g, '\\u0026');

function offlineStyles(fonts) {
  return `
@font-face{font-family:Editorial;src:url(data:font/woff2;base64,${fonts.editorial}) format('woff2');font-style:normal;font-weight:300 700;font-display:swap}
@font-face{font-family:Editorial;src:url(data:font/woff2;base64,${fonts.editorialItalic}) format('woff2');font-style:italic;font-weight:300 700;font-display:swap}
@font-face{font-family:Body;src:url(data:font/woff2;base64,${fonts.body}) format('woff2');font-style:normal;font-weight:200 800;font-display:swap}
:root{--forest:#173f35;--leaf:#496b56;--gold:#b69a58;--gold-light:#d9bf85;--gold-ink:#806624;--sage:#a6b49a;--ivory:#f5f1e8;--paper:#edece2;--white:#fffdf7;--ink:#102d26;--line:rgb(23 63 53/.2);--serif:Editorial,Georgia,serif;--sans:Body,Arial,sans-serif;--gutter:clamp(18px,5vw,72px)}
*{box-sizing:border-box}html{scroll-behavior:smooth;scroll-padding-top:90px}body{margin:0;background:var(--ivory);color:var(--ink);font:400 14px/1.75 var(--sans);-webkit-font-smoothing:antialiased}img,svg{display:block;max-width:100%}button,input{font:inherit;color:inherit}button,a{touch-action:manipulation}a{color:inherit}.shell{width:min(calc(100% - var(--gutter)*2),1240px);margin-inline:auto}.sr-only{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip-path:inset(50%);white-space:nowrap;border:0}:focus-visible{outline:2px solid var(--gold-ink);outline-offset:4px}h1,h2,h3,p{margin:0}h1,h2,h3{font-family:var(--serif);font-weight:400;line-height:.98;letter-spacing:-.04em}em{font-weight:inherit}.eyebrow{font-size:9px;font-weight:700;letter-spacing:.16em;text-transform:uppercase}.topbar{position:sticky;z-index:20;top:0;border-bottom:1px solid var(--line);background:rgb(245 241 232/.96);backdrop-filter:blur(10px)}.topbar__inner{display:flex;align-items:center;justify-content:space-between;gap:20px;min-height:72px}.brand{display:flex;align-items:center;gap:10px;text-decoration:none}.brand svg{width:28px;height:42px}.brand strong{font:400 24px/1 var(--serif)}.brand small{display:block;margin-top:5px;font-size:7px;letter-spacing:.18em;text-transform:uppercase}.topbar nav{display:flex;gap:6px}.topbar nav a{min-height:42px;display:flex;align-items:center;padding:8px 12px;font-size:9px;text-decoration:none}.hero{position:relative;overflow:hidden;background:var(--forest);color:var(--ivory)}.hero__grid{display:grid;grid-template-columns:.9fr 1.1fr;align-items:center;gap:6%;min-height:690px;padding-block:55px}.hero .eyebrow{color:var(--gold-light)}.hero h1{max-width:680px;margin-top:24px;font-size:clamp(58px,7vw,100px);line-height:.87}.hero h1 em{color:var(--sage)}.hero__copy>p:not(.eyebrow){max-width:520px;margin-top:25px;color:#d0d8cd}.hero__stats{display:flex;gap:10px 22px;flex-wrap:wrap;margin-top:28px;padding-top:20px;border-top:1px solid rgb(245 241 232/.24);font-size:9px;letter-spacing:.12em;text-transform:uppercase}.hero__art{position:relative;min-width:0;aspect-ratio:1/1}.hero__art:before,.hero__art:after{content:'';position:absolute;border:1px solid rgb(217 191 133/.42);border-radius:50%}.hero__art:before{inset:2% 7%}.hero__art:after{inset:8% 13%;transform:rotate(18deg)}.hero__art img{position:absolute;z-index:2;inset:10% 5% 6% 12%;width:83%;height:84%;object-fit:cover;border-radius:2px 44% 2px 2px;box-shadow:0 30px 80px rgb(0 0 0/.32)}.hero__art .cards{position:absolute;z-index:1;inset:7% 8% 4% 8%;transform:rotate(5deg);border:1px solid var(--gold);background:var(--ivory)}.intro{padding-block:90px}.intro__grid{display:grid;grid-template-columns:1fr .75fr;gap:12%;align-items:start}.intro h2{font-size:clamp(48px,6vw,82px)}.intro h2 em{color:var(--gold-ink)}.intro__grid>div:last-child{padding-top:25px;color:var(--leaf)}.toolbar{position:sticky;z-index:18;top:72px;border-block:1px solid var(--line);background:rgb(237 236 226/.97);backdrop-filter:blur(10px)}.toolbar__inner{display:grid;grid-template-columns:1fr auto auto;align-items:center;gap:18px;min-height:72px}.search{display:flex;align-items:center;min-height:42px;max-width:420px;padding-inline:14px;border:1px solid var(--line);background:var(--white)}.search input{width:100%;border:0;outline:0;background:transparent;font-size:11px}.filters,.toolbar__actions{display:flex;gap:5px}.filters button,.toolbar__actions button{min-height:38px;padding:7px 13px;border:1px solid var(--line);background:transparent;font-size:8px;letter-spacing:.08em;text-transform:uppercase;cursor:pointer}.filters button[aria-pressed=true],.toolbar__actions button:hover{background:var(--forest);color:var(--ivory)}.journey{padding-block:52px}.journey__layout{display:grid;grid-template-columns:275px minmax(0,1fr);gap:60px}.recipe-nav{position:sticky;top:165px;align-self:start;border-top:1px solid var(--line)}.recipe-nav a{display:grid;grid-template-columns:26px 1fr 24px;align-items:center;gap:8px;min-height:72px;padding:10px 7px;border-bottom:1px solid var(--line);text-decoration:none}.recipe-nav a[aria-current=true]{background:var(--paper)}.recipe-nav a[hidden]{display:none}.recipe-nav small{color:var(--gold-ink);font-size:8px}.recipe-nav strong{font:400 18px/1.05 var(--serif)}.recipe-nav em{display:block;margin-top:4px;color:var(--leaf);font:7px/1 var(--sans);letter-spacing:.1em;text-transform:uppercase}.recipe-nav .favorite-indicator{font-style:normal;color:var(--gold-ink);opacity:0}.recipe-nav a[data-favorite=true] .favorite-indicator{opacity:1}.recipe{scroll-margin-top:165px;margin-bottom:70px}.enhanced .recipe[hidden]{display:none}.recipe__hero{display:grid;grid-template-columns:.8fr 1fr;min-height:450px;background:var(--paper)}.recipe__hero img{width:100%;height:100%;object-fit:cover}.recipe__head{display:flex;flex-direction:column;justify-content:center;padding:clamp(28px,5vw,62px)}.recipe__head>.eyebrow{color:var(--gold-ink)}.recipe__head h2{margin-top:16px;font-size:clamp(48px,5vw,74px)}.recipe__head>p:not(.eyebrow){margin-top:20px;color:var(--leaf);font-size:12px}.recipe__facts{display:grid;grid-template-columns:1fr 1fr;border:1px solid var(--line);border-top:0}.recipe__facts>div{padding:18px}.recipe__facts>div+div{border-left:1px solid var(--line)}.recipe__facts span{display:block;color:var(--gold-ink);font-size:7px;letter-spacing:.1em;text-transform:uppercase}.recipe__facts strong{display:block;margin-top:5px;font-size:10px}.recipe__body{display:grid;grid-template-columns:.9fr 1.1fr;gap:55px;padding-block:55px}.section-title .eyebrow{color:var(--gold-ink)}.section-title h3{margin-top:9px;font-size:42px}.portion{display:flex;flex-wrap:wrap;gap:4px;margin-top:18px}.portion button{width:42px;height:38px;border:1px solid var(--line);background:transparent;font-size:9px;cursor:pointer}.portion button[aria-pressed=true]{background:var(--forest);color:var(--ivory)}.check-list,.steps,.allergens{padding:0;margin:24px 0 0;list-style:none;border-top:1px solid var(--line)}.check-list li,.steps li{border-bottom:1px solid var(--line)}.check-list label{display:grid;grid-template-columns:22px 1fr;gap:12px;padding-block:12px;color:var(--leaf);font-size:11px;cursor:pointer}.check-list input{position:absolute;opacity:0}.box{display:grid;place-items:center;width:20px;height:20px;border:1px solid var(--line)}.box:after{content:'✓';color:var(--ivory);opacity:0}.check-list input:checked+.box{background:var(--forest)}.check-list input:checked+.box:after{opacity:1}.check-list input:checked~span:last-child{text-decoration:line-through;opacity:.55}.check-list small{margin-left:8px;color:#9f5e3f;font-size:7px;text-transform:uppercase}.recipe__shopping{width:100%;min-height:46px;margin-top:20px;border:1px solid var(--forest);background:transparent;font-size:9px;cursor:pointer}.recipe__shopping[aria-pressed=true]{background:var(--forest);color:var(--ivory)}.substitution{margin-top:18px;padding:16px;border-left:2px solid var(--gold);background:var(--paper);color:var(--leaf);font-size:10px}.steps button{display:grid;grid-template-columns:30px 1fr 24px;gap:12px;width:100%;padding:17px 0;border:0;background:transparent;text-align:left;cursor:pointer}.steps button>span:first-child{color:var(--gold-ink);font-size:9px}.steps p{font-size:11px}.step-check{display:grid;place-items:center;width:23px;height:23px;border:1px solid var(--line);border-radius:50%;font-size:12px;color:transparent}.steps button[aria-pressed=true] p{text-decoration:line-through;opacity:.55}.steps button[aria-pressed=true] .step-check{background:var(--forest);color:var(--ivory)}.recipe__notes{display:grid;grid-template-columns:.75fr 1.25fr;border:1px solid var(--line)}.recipe__notes>section{padding:28px}.recipe__notes>section+section{border-left:1px solid var(--line)}.recipe__notes h3{font:700 9px/1.4 var(--sans);letter-spacing:.1em;text-transform:uppercase}.recipe__notes section>p{margin-top:12px;color:var(--leaf);font-size:10px}.allergens{display:grid;grid-template-columns:1fr 1fr;gap:16px;border:0}.allergens li{display:grid;gap:4px}.allergens strong{font-size:9px}.allergens span{color:var(--leaf);font-size:8px}.editorial-context{padding:16px 0;color:var(--leaf);font-size:9px;font-style:italic}.recipe__controls{display:flex;flex-wrap:wrap;gap:7px;margin-top:24px}.recipe__controls button{min-height:40px;padding:8px 13px;border:1px solid var(--line);background:transparent;font-size:8px;cursor:pointer}.recipe__controls button[aria-pressed=true]{background:var(--forest);color:var(--ivory)}.shopping{padding-block:85px;background:var(--paper)}.shopping__head{display:flex;align-items:end;justify-content:space-between;gap:30px;padding-bottom:30px;border-bottom:1px solid var(--line)}.shopping h2{font-size:clamp(48px,6vw,76px)}.shopping__head p{max-width:510px;margin-top:15px;color:var(--leaf)}.shopping__grid{display:grid;grid-template-columns:repeat(3,1fr);gap:38px;padding-top:35px}.shopping__grid h3{padding-bottom:10px;border-bottom:1px solid var(--gold);font:700 9px/1.4 var(--sans);letter-spacing:.1em;text-transform:uppercase}.shopping__grid ul{padding:0;margin:0;list-style:none}.shopping__empty{padding-top:28px;color:var(--leaf)}.about{padding-block:90px;background:var(--sage)}.about__grid{display:grid;grid-template-columns:1fr .8fr;gap:12%;align-items:end}.about h2{margin-top:18px;font-size:clamp(48px,6vw,80px)}.about h2 em{color:var(--forest)}.about__grid>div:last-child{color:var(--forest)}.about__identity{margin-top:22px;padding-top:18px;border-top:1px solid rgb(23 63 53/.25);font-size:11px}.notice{padding-block:26px;background:var(--forest);color:#ccd6c9;text-align:center;font-size:9px}.no-results{padding:30px;border:1px solid var(--line);color:var(--leaf)}
.toolbar__actions a{display:inline-flex;align-items:center;min-height:38px;padding:7px 13px;border:1px solid var(--line);font-size:8px;letter-spacing:.08em;text-decoration:none;text-transform:uppercase}.toolbar__actions a:hover{background:var(--forest);color:var(--ivory)}
.search:focus-within{outline:2px solid var(--gold-ink);outline-offset:3px}.search input{font-size:13px}.filters button,.toolbar__actions button,.toolbar__actions a{min-height:44px;font-size:11px}.recipe__head>p:not(.eyebrow){font-size:14px}.recipe__facts span{font-size:10px}.recipe__facts strong{font-size:13px}.portion button{width:44px;height:44px;font-size:12px}.check-list label{font-size:13px}.check-list small{color:#925538}.check-list input:focus-visible+.box{outline:2px solid var(--gold-ink);outline-offset:4px}.recipe__shopping{font-size:11px}.steps p{font-size:14px}.recipe__notes section>p,.allergens span,.editorial-context{font-size:12px}.allergens strong{font-size:12px}.recipe__controls button{min-height:44px;font-size:11px}
@media(max-width:900px){.hero__grid{grid-template-columns:1fr;min-height:0}.hero__art{width:min(100%,560px);margin-inline:auto}.intro__grid,.recipe__body,.about__grid{grid-template-columns:1fr}.journey__layout{grid-template-columns:minmax(0,1fr)}.journey__layout aside{min-width:0}.toolbar__inner{grid-template-columns:1fr auto;padding-block:12px}.toolbar__actions{grid-column:1/-1}.recipe-nav{position:static;display:grid;grid-template-columns:repeat(7,210px);max-width:100%;overflow-x:auto}.recipe__hero{grid-template-columns:1fr}.recipe__hero img{aspect-ratio:4/4.4}.recipe__facts{grid-template-columns:1fr}.recipe__facts>div+div{border-left:0;border-top:1px solid var(--line)}.shopping__grid{grid-template-columns:1fr 1fr}}
@media(max-width:600px){.topbar nav a{display:none}.hero h1{font-size:clamp(55px,16vw,78px)}.intro,.about{padding-block:65px}.toolbar{top:72px}.toolbar__inner{grid-template-columns:1fr}.filters{width:100%}.filters button{flex:1}.toolbar__actions{grid-column:auto;overflow-x:auto}.recipe__head{padding:28px 22px}.recipe__notes{grid-template-columns:1fr}.recipe__notes>section+section{border-left:0;border-top:1px solid var(--line)}.allergens,.shopping__grid{grid-template-columns:1fr}.shopping__head{align-items:start;flex-direction:column}.hero__art{margin-inline:-8px}.hero__art img{inset:8% 4% 6% 9%;width:87%}.section-title h3{font-size:38px}}
@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}*,*:before,*:after{animation-duration:.01ms!important;transition-duration:.01ms!important}}
@media print{@page{size:A4;margin:14mm}.topbar,.toolbar,.recipe-nav,.recipe__controls,.recipe__shopping,.portion,.shopping__head button{display:none!important}body{background:#fff;color:#000;font-size:10pt}.shell{width:100%}.hero{color:#000;background:#fff;break-after:page}.hero__grid{min-height:0}.hero__art{max-height:125mm}.intro{break-after:page}.journey{padding:0}.journey__layout{display:block}.recipe,.enhanced .recipe[hidden]{display:block!important;break-before:page;margin:0}.recipe__hero{grid-template-columns:70mm 1fr;min-height:85mm}.recipe__body{grid-template-columns:1fr 1fr;gap:10mm;padding-block:10mm}.recipe__notes{break-inside:avoid}.shopping{break-before:page}.about{break-before:page;background:#fff}.check-list input:checked~span:last-child,.steps button[aria-pressed=true] p{text-decoration:none;opacity:1}}
`;
}

function ingredientMarkup(ingredient, recipeId) {
  const label = formatIngredient(ingredient);
  const data = ingredient.quantity == null ? '' : ` data-quantity="${ingredient.quantity}" data-unit-one="${escapeHtml(ingredient.unit.singular)}" data-unit-many="${escapeHtml(ingredient.unit.plural)}" data-name="${escapeHtml(ingredient.name)}"`;
  return `<li><label><input type="checkbox" data-kind="ingredient" data-recipe="${recipeId}" data-item="${escapeHtml(ingredient.id)}"><span class="box" aria-hidden="true"></span><span data-ingredient-label${data}>${escapeHtml(label)}${ingredient.optional ? '<small>Opcional</small>' : ''}</span></label></li>`;
}

function recipeMarkup(recipe, index, imageData) {
  const searchable = [recipe.name, recipe.introduction, ...recipe.tags, ...recipe.ingredients.map((item) => item.display || `${item.name} ${item.unit?.singular || ''}`)].join(' ').toLocaleLowerCase('pt-BR');
  return `<article class="recipe" id="${recipe.slug}" data-recipe="${recipe.id}" data-category="${recipe.category}" data-search="${escapeHtml(searchable)}">
  <div class="recipe__hero"><img src="${imageData[recipe.slug]}" width="1000" height="1500" alt="${escapeHtml(recipe.image.alt)}"><div class="recipe__head"><p class="eyebrow">${escapeHtml(recipe.category)} · 0${index + 1}/07</p><h2>${escapeHtml(recipe.name)}</h2><p>${escapeHtml(recipe.introduction)}</p><div class="recipe__controls"><button type="button" data-favorite="${recipe.id}" aria-pressed="false">♡ Favoritar</button><button type="button" data-prepared="${recipe.id}" aria-pressed="false">✓ Marcar como preparada</button></div></div></div>
  <div class="recipe__facts"><div><span>Tempo</span><strong>${escapeHtml(recipe.time.label)}${recipe.time.inferred ? ' · definição editorial a confirmar' : ''}</strong></div><div><span>Equipamentos</span><strong>${escapeHtml(recipe.equipment.join(' · '))}</strong></div></div>
  <div class="recipe__body"><section><div class="section-title"><p class="eyebrow">Checklist</p><h3>Ingredientes</h3></div><div class="portion" aria-label="Ajustar quantidade">${[0.5, 1, 1.5, 2].map((value) => `<button type="button" data-multiplier="${value}" data-recipe="${recipe.id}" aria-pressed="${value === 1}">${value === 0.5 ? '½' : value === 1.5 ? '1½' : value}×</button>`).join('')}</div><ul class="check-list">${recipe.ingredients.map((ingredient) => ingredientMarkup(ingredient, recipe.id)).join('')}</ul>${recipe.substitutions.length ? `<div class="substitution"><strong>Alternativa</strong>${recipe.substitutions.map((item) => `<p>${escapeHtml(item)}</p>`).join('')}</div>` : ''}<button class="recipe__shopping" type="button" data-shopping="${recipe.id}" aria-pressed="false">Adicionar à lista de compras</button></section>
  <section><div class="section-title"><p class="eyebrow">Passo a passo</p><h3>Modo de preparo</h3></div><ol class="steps">${recipe.preparation.map((step, stepIndex) => `<li><button type="button" data-kind="step" data-recipe="${recipe.id}" data-item="${stepIndex}" aria-pressed="false"><span>${String(stepIndex + 1).padStart(2, '0')}</span><p>${escapeHtml(step)}</p><span class="step-check">✓</span></button></li>`).join('')}</ol></section></div>
  <div class="recipe__notes"><section><h3>Observações</h3>${recipe.notes.map((note) => `<p>${escapeHtml(note)}</p>`).join('')}</section><section><h3>Alergênicos e cuidados</h3><ul class="allergens">${recipe.allergens.map((allergen) => `<li><strong>${escapeHtml(allergen.label)}</strong><span>${escapeHtml(allergen.detail)}</span></li>`).join('')}</ul></section></div><p class="editorial-context">${escapeHtml(recipe.editorialContext)}</p>
  </article>`;
}

function offlineApp() {
  document.documentElement.classList.add('enhanced');
  const DATA = JSON.parse(document.getElementById('product-data').textContent);
  const prefix = 'gislaine:receitas:offline:v1:';
  const memory = new Map();
  const read = (key, fallback) => { try { const value = localStorage.getItem(prefix + key); return value == null ? fallback : JSON.parse(value); } catch { return memory.has(key) ? memory.get(key) : fallback; } };
  const write = (key, value) => { memory.set(key, value); try { localStorage.setItem(prefix + key, JSON.stringify(value)); } catch { /* fallback em memória */ } };
  const toggle = (list, item) => list.includes(item) ? list.filter((entry) => entry !== item) : [...list, item];
  const fractions = (value) => { const rounded = Math.round(value * 4) / 4; const whole = Math.floor(rounded); const part = ['', '¼', '½', '¾'][Math.round((rounded - whole) * 4)] || ''; return whole ? (part ? whole + ' ' + part : String(whole)) : (part || String(rounded)); };
  let favorites = read('favorites', []);
  let prepared = read('prepared', []);
  let shopping = read('shopping', []);
  let checks = read('checks', {});
  let multipliers = read('multipliers', {});
  let shoppingChecks = read('shopping-checks', []);
  let current = location.hash.slice(1) || DATA.recipes[0].slug;

  const refreshButtons = () => {
    document.querySelectorAll('[data-favorite]').forEach((button) => { const active = favorites.includes(button.dataset.favorite); button.setAttribute('aria-pressed', active); button.textContent = active ? '♥ Favorita' : '♡ Favoritar'; });
    document.querySelectorAll('[data-prepared]').forEach((button) => { const active = prepared.includes(button.dataset.prepared); button.setAttribute('aria-pressed', active); button.textContent = active ? '✓ Já preparei' : '✓ Marcar como preparada'; });
    document.querySelectorAll('[data-shopping]').forEach((button) => { const active = shopping.includes(button.dataset.shopping); button.setAttribute('aria-pressed', active); button.textContent = active ? 'Na lista de compras' : 'Adicionar à lista de compras'; });
    document.querySelectorAll('[data-nav]').forEach((link) => { link.dataset.favorite = favorites.includes(link.dataset.id); });
    document.getElementById('progress-value').textContent = prepared.length;
  };
  const refreshChecks = () => document.querySelectorAll('[data-kind]').forEach((control) => { const key = control.dataset.recipe + ':' + control.dataset.kind; const active = (checks[key] || []).includes(control.dataset.item); if (control.type === 'checkbox') control.checked = active; else control.setAttribute('aria-pressed', active); });
  const showRecipe = (slug, shouldScroll = true) => {
    const target = document.querySelector('[data-recipe][id="' + CSS.escape(slug) + '"]');
    if (!target) return;
    current = slug;
    document.querySelectorAll('.recipe').forEach((article) => { article.hidden = article !== target; });
    document.querySelectorAll('[data-nav]').forEach((link) => link.setAttribute('aria-current', String(link.getAttribute('href') === '#' + slug)));
    history.replaceState(null, '', '#' + slug);
    if (shouldScroll) target.scrollIntoView({ block: 'start', behavior: matchMedia('(prefers-reduced-motion: reduce)').matches ? 'auto' : 'smooth' });
  };
  const applyMultiplier = (recipeId, value) => {
    multipliers = { ...multipliers, [recipeId]: value }; write('multipliers', multipliers);
    document.querySelectorAll('[data-multiplier][data-recipe="' + recipeId + '"]').forEach((button) => button.setAttribute('aria-pressed', String(Number(button.dataset.multiplier) === value)));
    document.querySelector('[data-recipe="' + recipeId + '"]').querySelectorAll('[data-ingredient-label][data-quantity]').forEach((label) => {
      const amount = Number(label.dataset.quantity) * value;
      const unit = amount <= 1 ? label.dataset.unitOne : label.dataset.unitMany;
      const optional = label.querySelector('small')?.outerHTML || '';
      label.innerHTML = [fractions(amount), unit, label.dataset.name].filter(Boolean).join(' ') + optional;
    });
    renderShopping();
  };
  const renderShopping = () => {
    const root = document.getElementById('shopping-content');
    const entries = new Map();
    DATA.recipes.filter((recipe) => shopping.includes(recipe.id)).forEach((recipe) => recipe.ingredients.forEach((ingredient) => {
      const key = ingredient.shoppingKey + ':' + (ingredient.unit?.singular || 'display');
      const currentEntry = entries.get(key);
      if (ingredient.quantity == null) { if (!currentEntry) entries.set(key, { ...ingredient, key }); }
      else if (currentEntry) currentEntry.quantity += ingredient.quantity * (multipliers[recipe.id] || 1);
      else entries.set(key, { ...ingredient, key, quantity: ingredient.quantity * (multipliers[recipe.id] || 1) });
    }));
    if (!entries.size) { root.innerHTML = '<p class="shopping__empty">Adicione uma receita para montar sua lista.</p>'; return; }
    const groups = {};
    entries.forEach((item) => { (groups[item.section] ||= []).push(item); });
    root.innerHTML = '<div class="shopping__grid">' + Object.entries(groups).map(([section, items]) => '<section><h3>' + section + '</h3><ul class="check-list">' + items.map((item) => { const amount = item.quantity == null ? item.display : [fractions(item.quantity), item.quantity <= 1 ? item.unit.singular : item.unit.plural, item.name].filter(Boolean).join(' '); return '<li><label><input type="checkbox" data-shopping-check="' + item.key + '" ' + (shoppingChecks.includes(item.key) ? 'checked' : '') + '><span class="box" aria-hidden="true"></span><span>' + amount + (item.optional ? '<small>Opcional</small>' : '') + '</span></label></li>'; }).join('') + '</ul></section>').join('') + '</div>';
    root.querySelectorAll('[data-shopping-check]').forEach((input) => input.addEventListener('change', () => { shoppingChecks = toggle(shoppingChecks, input.dataset.shoppingCheck); write('shopping-checks', shoppingChecks); }));
  };
  const filter = (shouldScroll = true) => {
    const query = document.getElementById('search').value.trim().toLocaleLowerCase('pt-BR');
    const category = document.querySelector('[data-filter][aria-pressed=true]').dataset.filter;
    let first = null;
    document.querySelectorAll('[data-nav]').forEach((link) => { const article = document.getElementById(link.getAttribute('href').slice(1)); const visible = (category === 'todas' || article.dataset.category === category) && (!query || article.dataset.search.includes(query)); link.hidden = !visible; if (visible && !first) first = article; });
    document.getElementById('no-results').hidden = Boolean(first);
    const active = document.getElementById(current);
    if (!first) document.querySelectorAll('.recipe').forEach((article) => { article.hidden = true; });
    else if (!active || active.hidden || document.querySelector('[data-nav][href="#' + CSS.escape(current) + '"]').hidden) showRecipe(first.id, shouldScroll);
  };

  document.querySelectorAll('[data-nav]').forEach((link) => link.addEventListener('click', (event) => { event.preventDefault(); showRecipe(link.getAttribute('href').slice(1)); }));
  document.querySelectorAll('[data-favorite]').forEach((button) => button.addEventListener('click', () => { favorites = toggle(favorites, button.dataset.favorite); write('favorites', favorites); refreshButtons(); }));
  document.querySelectorAll('[data-prepared]').forEach((button) => button.addEventListener('click', () => { prepared = toggle(prepared, button.dataset.prepared); write('prepared', prepared); refreshButtons(); }));
  document.querySelectorAll('[data-shopping]').forEach((button) => button.addEventListener('click', () => { shopping = toggle(shopping, button.dataset.shopping); write('shopping', shopping); refreshButtons(); renderShopping(); }));
  document.querySelectorAll('[data-kind]').forEach((control) => control.addEventListener('change', () => { const key = control.dataset.recipe + ':' + control.dataset.kind; checks = { ...checks, [key]: toggle(checks[key] || [], control.dataset.item) }; write('checks', checks); refreshChecks(); }));
  document.querySelectorAll('.steps [data-kind]').forEach((button) => button.addEventListener('click', () => { const key = button.dataset.recipe + ':' + button.dataset.kind; checks = { ...checks, [key]: toggle(checks[key] || [], button.dataset.item) }; write('checks', checks); refreshChecks(); }));
  document.querySelectorAll('[data-multiplier]').forEach((button) => button.addEventListener('click', () => applyMultiplier(button.dataset.recipe, Number(button.dataset.multiplier))));
  document.querySelectorAll('[data-filter]').forEach((button) => button.addEventListener('click', () => { document.querySelectorAll('[data-filter]').forEach((item) => item.setAttribute('aria-pressed', String(item === button))); filter(); }));
  document.getElementById('search').addEventListener('input', () => filter(false));
  document.getElementById('add-all').addEventListener('click', () => { shopping = DATA.recipes.map((recipe) => recipe.id); write('shopping', shopping); refreshButtons(); renderShopping(); });
  document.getElementById('clear-shopping').addEventListener('click', () => { shopping = []; shoppingChecks = []; write('shopping', shopping); write('shopping-checks', shoppingChecks); refreshButtons(); renderShopping(); });
  document.getElementById('print').addEventListener('click', () => window.print());
  DATA.recipes.forEach((recipe) => applyMultiplier(recipe.id, multipliers[recipe.id] || 1));
  refreshButtons(); refreshChecks(); renderShopping(); filter(false);
  const initial = document.getElementById(current) ? current : DATA.recipes[0].slug;
  showRecipe(initial, false);
}

export function buildOfflineHtml({ data, imageData, heroData, fonts, brandSvg }) {
  const recipes = data.recipes;
  const offlineData = {
    ...data,
    recipes: recipes.map((recipe) => ({
      ...recipe,
      image: { alt: recipe.image.alt, width: recipe.image.width, height: recipe.image.height },
    })),
  };
  const navigation = recipes.map((recipe, index) => `<a href="#${recipe.slug}" data-nav data-id="${recipe.id}" data-favorite="false"><small>0${index + 1}</small><span><strong>${escapeHtml(recipe.name)}</strong><em>${escapeHtml(recipe.category)}</em></span><span class="favorite-indicator" aria-label="Favorita">♥</span></a>`).join('');
  const recipesHtml = recipes.map((recipe, index) => recipeMarkup(recipe, index, imageData)).join('');
  return `<!doctype html>
<html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="color-scheme" content="light"><meta name="referrer" content="no-referrer"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; img-src data:; font-src data:; style-src 'unsafe-inline'; script-src 'unsafe-inline'; connect-src 'none'; object-src 'none'; base-uri 'none'; form-action 'none'"><title>${escapeHtml(data.title)} · versão offline</title><meta name="description" content="${escapeHtml(data.subtitle)}"><style>${offlineStyles(fonts)}</style></head>
<body><a class="sr-only" href="#conteudo">Pular para o conteúdo</a><header class="topbar"><div class="shell topbar__inner"><a class="brand" href="#inicio">${brandSvg}<span><strong>Gislaine Duarte</strong><small>Nutricionista · ${escapeHtml(data.shortTitle)}</small></span></a><nav aria-label="Navegação principal"><a href="#receitas">Receitas</a><a href="#lista">Lista de compras</a><a href="#sobre">Sobre a Nutri Gi</a></nav></div></header>
<main id="conteudo"><section class="hero" id="inicio"><div class="shell hero__grid"><div class="hero__copy"><p class="eyebrow">Coleção digital de receitas</p><h1>7 receitas para ajudar você a <em>desinflamar!</em></h1><p>${escapeHtml(data.subtitle)}</p><div class="hero__stats"><span>7 receitas</span><span>funciona sem internet</span><span>preferências locais</span></div></div><div class="hero__art" aria-hidden="true"><div class="cards"></div><img src="${heroData}" width="1200" height="800" alt=""></div></div></section>
<section class="intro"><div class="shell intro__grid"><div><p class="eyebrow">Como usar</p><h2>Escolha, organize<br />e prepare no <em>seu ritmo.</em></h2></div><div><p>Pesquise por receita ou ingrediente, filtre entre doces e salgadas e marque o que já preparou. Ajustes de quantidade alteram apenas medidas numéricas; o ponto e o tempo ainda devem ser observados.</p><p>Monte a lista de compras a partir das receitas escolhidas. Seus favoritos, checklists e progresso ficam somente neste arquivo, no armazenamento local do navegador quando disponível.</p></div></div></section>
<section class="toolbar" aria-label="Ferramentas"><div class="shell toolbar__inner"><label class="search"><span class="sr-only">Pesquisar receita ou ingrediente</span><input id="search" type="search" placeholder="Buscar receita ou ingrediente"></label><div class="filters" aria-label="Filtrar categoria"><button type="button" data-filter="todas" aria-pressed="true">Todas</button><button type="button" data-filter="doce" aria-pressed="false">Doces</button><button type="button" data-filter="salgada" aria-pressed="false">Salgadas</button></div><div class="toolbar__actions"><button id="print" type="button">Imprimir</button><a href="#lista">Lista de compras</a></div></div></section>
<section class="journey" id="receitas"><div class="shell journey__layout"><aside><nav class="recipe-nav" aria-label="As sete receitas">${navigation}</nav></aside><div><p id="no-results" class="no-results" hidden>Nenhuma receita corresponde à busca. Tente outro termo ou remova o filtro.</p>${recipesHtml}</div></div></section>
<section class="shopping" id="lista"><div class="shell"><div class="shopping__head"><div><p class="eyebrow">Organização</p><h2>Lista de compras</h2><p>As medidas numéricas iguais são somadas quando usam a mesma unidade. Ingredientes “a gosto” permanecem sem quantidade.</p></div><div><button id="add-all" type="button">Adicionar todas</button> <button id="clear-shopping" type="button">Limpar</button></div></div><div id="shopping-content"><p class="shopping__empty">Adicione uma receita para montar sua lista.</p></div></div></section>
<section class="about" id="sobre"><div class="shell about__grid"><div><p class="eyebrow">Por Gislaine Duarte</p><h2>Ciência, escuta<br />e <em>vida real.</em></h2></div><div><p>Sou Gislaine Duarte, nutricionista, gaúcha, cristã, esposa e empreendedora. Meu trabalho é voltado a mulheres e famílias que desejam construir uma relação mais consciente com a alimentação e buscar mais qualidade de vida.</p><div class="about__identity"><strong>Gislaine Muller Duarte</strong><br>Nutricionista · CRN-2 nº 22562<br>gislaineduarte.com.br</div></div></div></section></main>
<footer class="notice"><div class="shell">${escapeHtml(data.educationalNotice)}</div></footer>
<script id="product-data" type="application/json">${safeJson(offlineData)}</script><script>(${offlineApp.toString()})();</script></body></html>`;
}
